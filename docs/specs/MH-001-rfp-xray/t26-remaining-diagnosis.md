# T26 remaining gaps after the diagnostic capacity change

These observations are from unchanged genuine v8 cached responses, not a new
model call and not authorization to alter semantic safeguards.

- The four closing-date Claims contain date-only text (2026-09-03 or2026-09-15)
  and date-only evidence such as the cover's On field. No full date/time/timezone
  Claim was generated. The base Claim passes authority but fails field binding;
  amendment date Claims also receive relationless-submission or duplicate-
  relevance disagreement. Merely raising capacity cannot fill the missing time.
- No source Claim contains2050 or2055; the only horizon-bearing Requirement
  states the amendment table's2050/2055/2060 wording. It is discarded downstream
  for prose/category support. The required three-source inconsistency evidence
  is absent from the generated source-Claim set, not merely hidden by the cap.
- The model generates only four amendment003 mandatory table rows, without row
  section labels. It does not generate the full37old+37new row inventory or an
  atomic complete-replacement Claim. Therefore no honest offline decoder fix can
  certify complete M3 replacement using these responses alone.
- Broad contract obligations are frequently classified mandatory by the model,
  while the materializer requires a mandatory evaluation marker/section. Keep
  this semantic-taxonomy mismatch separate from physical quote positioning.

After T27, reframe missing extraction coverage before implementation. Any next
provider experiment should use the already saved Monid text and only the relevant
amendment/table/cover content, with a narrowly specified extraction objective and
raw-response capture. Do not reparse all PDFs or restart the full pipeline.
Do not claim a full CER pass from recovered evaluation rules alone.
