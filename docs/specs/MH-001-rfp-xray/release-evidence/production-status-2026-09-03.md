# Production Status — 2026-09-03

Updated at `2026-09-03T21:27:52Z`.

## Release identity

- Release Git SHA: `76e0f4e01f93d67eab4da9b98807959b81578396`.
- Application-code parent: `fbb48d09bda4f8d671f6b1679c66d3e0400f45db`.
- GitHub Actions run: `33793276409`, successful.
- Vercel production deployment: `dpl_5dMrPWKGMCKxy5hcQUfq57uLmZce`.
- Immutable deployment URL:
  `https://rfp-xray-3dpwofwgr-chendavs-projects.vercel.app`.
- Public alias: `https://rfp-xray.vercel.app`.

The local, reviewed implementation is
`4089397de8f2cfc3dc4846911bd9767adea178f4` and remains unpushed. The
Cloudflare Managed Turnstile widget allowlists only the public alias; its site
key, sensitive secret, and exact expected hostname are configured in Vercel
Production. Those values are not present in the older immutable deployment
listed above and require one new deployment.

## Deployment-bound receipts

- Runtime receipt payload SHA-256:
  `5d50e812e28ee43fdc81bd99c8a2a291a737ff3c607ccb2d148cbba97aa14dbf`;
  expires `2026-09-04T19:00:58.845Z`.
- Monid/OpenAI provider-contract receipt payload SHA-256:
  `0c8ede2c44fc3ff8038eea7640573bdef5cbbb0523ae7583e66b5e8f1743fe07`;
  expires `2026-09-04T19:01:09.386Z`.

Receipts are bound to this deployment and must not be copied to a later one.

## Public checks

The production Playwright smoke passed 4/4 against the public alias: landing,
OpenAPI, SHA-bound Edmonton sample, and truthful health behavior.

`GET /api/health` returned `503 not_ready`. Its dependencies reported:

- database: `ready`;
- Neon capacity: `attested`;
- maintenance: `fresh`;
- private storage: `attested`;
- Workflow: `attested_300s`;
- Monid: `actively_verified`;
- OpenAI: `actively_verified`;
- storage provider: `railway_s3`;
- storage safety: `current`.

The immutable deployment reports only this missing configuration because it
predates the now-configured project values:

- `TURNSTILE_SECRET_KEY`;
- `NEXT_PUBLIC_TURNSTILE_SITE_KEY`;
- `TURNSTILE_EXPECTED_HOSTNAME`.

This proves the release is correctly fail-closed. It does not prove a live
guest mutation, the eleven-run paid campaign, end-to-end recovery, production
citation click-through, measured campaign cost/latency, or contest completion.

## Isolated Preview recovery evidence

One provider-free Preview canary received a literal process `SIGKILL` and
completed through same-step Vercel Workflow redelivery. A read-only replay of
that same run recorded `workflow_start_count=0`, two ordered starts, one
materialized/output attempt 2, one completion, zero retry/failure events, and
no third attempt. Because Vercel omitted optional event attempt values, the
`[1,2]` sequence is explicitly labeled derived rather than directly observed.
A separate v2 log receipt, generated by reviewed commit `4089397`, corroborates
one literal `SIGKILL` record only at the deployment/window level. The row has
no raw run ID, so exact-run binding is false. Its hashes are local-receipt
pseudonymization, not anonymity; the earlier local v1 receipt is superseded. See
[workflow-recovery-canary-2026-09-03.md](workflow-recovery-canary-2026-09-03.md).

This closes only isolated platform-redelivery evidence. Full application
recovery, paid-provider behavior, cleanup timing, and production readiness
remain open.
