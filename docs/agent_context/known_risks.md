# Known Risks

Updated: 2026-09-04

| ID | Trigger | Impact | Mitigation | Owner | Status |
|---|---|---|---|---|---|
| R-1 | Monid parse schema, price, or retention drifts from the credentialed spike | Live pipeline or privacy claim becomes invalid | Pin the canonical inspect hash, require a fresh deployment-bound provider receipt, and disclose observed seven-day upstream retention while ZDR is unavailable | backend | live contract spike and current exact-deployment receipt passed; refresh after redeploy or expiry |
| R-2 | Railway private-object deletion or replay fencing fails in the end-to-end pipeline | Private source remains available and READY claim is false | Bound storage attestation is valid through 2026-09-10 04:11:53 MDT; S3 live 1/1 and real production-Origin Chromium probe 1/1 passed; retain fail-closed cleanup and verify end-to-end timing | backend | component contract passed; end-to-end gate open |
| R-3 | Model fabricates page numbers or merges unrelated clauses | Material procurement error | Model emits quote/chunk only; server attaches and verifies physical pages and relation roles | backend | mitigated-locally; live model gate open |
| R-4 | Amendment ordering or replacement is wrong | Superseded requirements appear active | Deterministic manifest/reconciliation rules, cross-document stale-risk checks, and CER golden suite | backend | mitigated-locally; live CER gate open |
| R-5 | Public demo abuse exhausts wallet | Unexpected spend or outage | Turnstile, signed session, per-principal quotas, transactional budget cap; do not enable live mutations until production keys are deployed and challenged | backend | widget and Vercel Production values configured; current deployment still blocks live mode pending redeploy |
| R-6 | Frontend and API contracts diverge during parallel work | Broken integration | Freeze Zod/OpenAPI types before dispatch; frontend consumes shared types | chief | open |
| R-7 | Missing release provider attestation or deployed Turnstile | Public sample exists, but real RFP processing cannot be released | Keep `/api/health` fail-closed; bind Monid/OpenAI checks to the exact deployment; require the real Turnstile challenge before public mutations | chief | widget/variables configured; redeployment, live challenge, and fresh receipts remain open |
| R-8 | Copyrighted tender files enter Git | Unauthorized redistribution and repository bloat | Commit only URLs, hashes, facts, and synthetic fixtures | chief | open |
| R-9 | Deadline-driven scope expansion | Core auditability remains unfinished | Preserve must-have gates; omit search, billing, accounts, collaboration | chief | open |
| R-10 | Direct copying from ontology lacks an explicit repository license | Legal and dependency risk | Port independently verified invariants and tests, not Python/Marker source or runtime | chief | mitigated |
| R-11 | Expiry/delete cleanup fails but state becomes expired or result remains readable | Source retention and analysis disclosure after a failed deletion | Gate result reads on clean terminal state; keep cleanup retryable; test failed expiry/delete and abandoned uploads | backend | mitigated-locally |
| R-12 | Production guest mutations lack client Turnstile tokens | Public Web is unusable or abuse protection is bypassed | Fresh action-bound token for each mutation; verify action/hostname server-side; production-path E2E | frontend/backend | mitigated-locally |
| R-13 | Model summary bypasses SHA/page/quote verification | Invented issuer/date/method appears authoritative | Require quote-level field anchors plus scalar/timezone agreement and adversarial tests | backend | mitigated-locally |
| R-14 | Requirement amendment lifecycle is not reconciled | Superseded requirements remain active | Server-order reconciliation, source-authorized mutations, CER materialization tests, and stale-risk filtering | backend | mitigated-locally |
| R-15 | Production silently selects memory/local adapters | Lost runs, bypassed quotas, and false health | Fail closed unless Workflow, Neon, validated private storage, Monid, OpenAI, and normalization paths are configured | backend | mitigated locally and verified fail-closed in the reviewed deployment |
| R-16 | OpenAI request/cost is not hard bounded | Context failure or spend above reservation | Bound bytes/tokens/output/cost and share one aggregate deadline across every batch | backend | mitigated-locally |
| R-17 | Monid artifact URL can target arbitrary HTTPS origins | Server-side request forgery | Pin inspected artifact origin and revalidate public-network redirects | backend | mitigated-locally |
| R-18 | Signed upload is abandoned or replayed after deletion | Untracked source retention | Durable incoming-upload expiry/sweeper plus replay-safe deletion design; live Railway and browser-origin replay probes passed | backend | live storage contract passed; end-to-end sweeper gate open |
| R-19 | Vulnerable runtime or release-tool dependencies | Known denial/disclosure vulnerabilities | Keep Vercel CLI 59.11.2 pinned, use scoped patched overrides, rerun production and full high/critical audits, and keep 33 attestation tests in the gate | backend | production audit clean; full audit 0 high/critical, 1 low/3 moderate development findings |
| R-20 | Process crashes after durable run creation but before Workflow start | Active slot remains queued and never analyzes | Idempotent replay plus indexed bounded maintenance admission recovery | backend | mitigated-locally; deployed crash test open |
| R-21 | Presign requests bypass run quotas and exhaust private storage | Cost or denial of service before run creation | Atomic owner/quota/global issuance locks with retained daily usage events | backend | live Neon concurrency probe passed; deployed flow open |
| R-22 | Concurrent or ambiguously acknowledged Workflow admission duplicates or destroys a run | Duplicate paid work, orphan workflow, or premature source cleanup | Permanent pre-start analysis-dispatch claim, one-way settlement, processing CAS, and maintenance-only terminal cleanup after uncertain acknowledgement | backend | ACK-loss fence independently approved P0/P1/P2=0; live Workflow gate open |
| R-23 | A model borrows adjacent labels or a false topic to mutate another field | Wrong title/deadline or silent supersession | Field-local spans, source-derived deadline keys, object-bound mutation clauses, needs-review fallback | backend | mitigated-locally |
| R-24 | Cleanup recreates a zero-byte fence after its durable upload ledger was swept | Permanent untracked private object | Ledger-first purge; absent ledger never writes and any orphan object is conditionally removed | backend | mitigated-locally; live S3 CAS/delete contract passed; end-to-end sweep open |
| R-25 | Conditional, negative, comparative, or adjacent prose lends authority to the wrong fact | Unsupported mandatory/deadline/selection/submission result | Field-local affirmative relation parsers, objective tuple completeness, and fail-closed needs-review output | backend | mitigated-locally; live model gate open |
| R-26 | A later document repeats a superseded scalar in a risk | Old deadline or amount remains actionable | Cross-document server-derived lineage and invalidated-scalar filtering | backend | mitigated-locally; live CER gate open |
| R-27 | The 300-second Vercel Fluid Compute design is presented as fully proven from one fast parse | Timeout, incomplete cleanup, misleading readiness, or failed demo | Bind runtime facts to the exact deployment, enforce 105/150/285-second internal deadlines, and require ten Edmonton runs plus one CER run; consider a Railway analysis worker only if those measurements fail | chief | one signed-URL parse completed/cleaned in 8.140s; full campaign open |
| R-28 | Read-only HTTP/Playwright smoke is mistaken for complete browser-level deployment QA | Turnstile, live mutation, or citation-link defects escape review | Use the authorized in-app browser for a real production challenge, full guest flow, and 12-citation click-through before release | reviewer | browser control available; production flow still open |
| R-29 | Production health HTTP 200 or migrated Neon schema is promoted as full contest completion | False release claim and unverifiable demo evidence | Keep component health separate from run quality, campaign, citation, video, and publication evidence | chief | active: health is 200, while the last Edmonton run was partial and contest gates remain open |
| R-30 | The audited paid verifier is mistaken for an executed live campaign | Fabricated cost, latency, cleanup, or provider evidence | Record verifier audit separately from execution; require its explicit paid gate and resulting sanitized campaign artifact | reviewer | verifier PASS P0=0/P1=0; paid path never run |
| R-31 | The public sample or healthy provider gate is mistaken for a successful analyzed run | Production behavior is overstated despite a partial Edmonton result | Label sample, component health, controlled runs, and final campaign as separate evidence classes | chief | active: production health is ready, but the last controlled Edmonton run ended partial |
| R-32 | A runtime receipt is copied between deployments or outlives inspected facts | Paid/source work runs under an unverified runtime | Bind the receipt to deployment ID/URL, project/team, Git SHA, Node/runtime/duration/memory/region, SDK and internal limits; fail closed on mismatch/expiry | backend | current production deployment is `dpl_2jKkhjjxeRGzq5yxgGS5nL3GJzJF`; refresh after any later deployment |
| R-33 | Provider catalog/configuration drifts after the live contract spike | Wrong endpoint, schema, price, model, or control-plane capability is trusted | Require a short-lived deployment-bound provider-contract attestation using credentialed non-paid inspect/control-plane checks | backend | inspect hash/config pinned; current exact-deployment Monid/OpenAI receipt passed and must be refreshed after Turnstile redeployment or expiry |
| R-38 | Context.dev retains parsed content while the app claims complete deletion | Confidential source persists upstream and privacy disclosure is false | Show the observed seven-day artifact expiry before submission and in audit; promise deletion only for app-controlled copies; require a new proof before changing the disclosure | chief | ZDR unavailable and seven-day expiry observed; UI/API disclosure deployed |
| R-39 | Monid Markdown is treated as physical-page truth | Citations point to invented or shifted pages | Keep the independent PDF.js physical index and exact-quote verifier authoritative; withhold OCR-only claims that cannot bind to a page | backend | live Monid output had no page signals; fail-closed regression added |
| R-40 | A worker is hard-killed after arming a source-cleanup watchdog but before recording the paid-provider start | An expired-lease replacement schedules a second package watchdog and breaks the per-run Workflow envelope | Permanently block processing reclaim once any watchdog exists; let maintenance expire and clean the run; enforce the same condition atomically in Neon | backend | fence independently APPROVED P0=0/P1=0; isolated Preview platform redelivery passed, but full application hard-kill cleanup remains open |
| R-41 | Vercel omits optional event attempt fields, rejects an oversized list page, or emits a SIGKILL log without a run ID | A valid recovery is rejected, or incomplete telemetry is overstated as direct evidence | Require two ordered same-step starts plus one completed materialized step/output at attempt 2; label the sequence derived; cap pages at 100; call log evidence exact-run-bound only when the unique SIGKILL row contains that run ID | backend | real read-only replay passed with `start_count=0`; log proof is explicitly deployment/window-bounded; independent reviews APPROVE P0=0/P1=0 |
| R-34 | A scheduled maintenance provider delays or drops invocations | Heartbeat becomes stale and new work is unavailable | Use offset GitHub and Railway triggers against one idempotent bounded route; require durable 15-minute freshness and fail closed | chief | Railway seven-cycle proof across more than 30 minutes and independent APPROVE; GitHub schedule delivery remains unobserved redundancy |
| R-35 | A component receipt expires during final submission work | Production health fails closed or evidence becomes stale | Refresh S3, runtime, and provider receipts under the scheduled heartbeat on Sep 9 and Sep 10 at 12:00 MDT | chief | scheduled; refresh still required |
| R-36 | Price screenshot is treated as competitor feature or service evidence | Misleading comparison claim | Preserve URL, capture time, bytes and SHA; claim only visible Starter £190/month and typical usage of one tender | chief | sanitized price evidence recorded |
| R-37 | Railway Cron expands into an unreviewed second application runtime | Duplicate queues, secret sprawl, and divergent analysis behavior | Keep the service no-domain, no-volume, zero-idle-instance, restart NEVER, and limited to maintenance URL/secret; never add Monid/OpenAI/DB/S3 credentials | chief | independently approved P0/P1/P2=0; monitor configuration drift |
| R-42 | A global submission boolean or reusable same-ID receipt admits an unrelated model record containing an unfamiliar contradictory channel | Email can resolve while a SecureDrop Claim/Risk remains authoritative | Bind authority to canonical record content independent of model IDs and revalidate a strict one-to-one mapping immediately before materialization | backend | mitigated locally; QA5 Revision 3 APPROVE |
| R-44 | Private-sidecar bytes are presented as full provider or server-receipt headroom | A passing sample assertion can hide output truncation or an oversized authority receipt | Keep provider token caps, control-plane byte bounds, and the independent 262144-byte server-receipt gate separate; require complete parse and empirical official runs | backend | QA5 Revision 1 closed the original P1 under the approved truthful reframe |
| R-45 | A server-recovered record collides with a model record ID and inherits its origin authority | Deterministic recovery can create a false submission conflict or veto | Recovered records always carry zero model origins and cannot enter model-authority lookup by public ID | backend | mitigated locally; QA5 Revision 3 APPROVE |
| R-46 | A five-batch packing target is described as a global call ceiling | Cost evidence understates per-request rounding for denser accepted plans | Compute and gate exact estimated cost from actual batch count N before dispatch; label official 3/5 as observed only | backend | mitigated locally; QA5 Revision 3 APPROVE |
| R-47 | Actual record-authority receipt metrics exist only in transient process memory | A successful paid run cannot produce durable empirical capacity evidence | Persist an allowlisted non-body audit record in the run row for 30 days and expose only a credentialed local operator reader by run ID | backend | mitigated locally; QA5 Revision 3 APPROVE, production evidence pending |
| R-43 | Legacy free-form review loops continue beyond the refreshed three-round ceiling | Repeated patches drift semantics and consume time without a stable design | Treat the current sequence as exhausted; require a bounded classifier redesign, then one independent review against the declared gate | chief | governance migration independently passed P0/P1/P2=0; enforce on T4 and future work |
| R-48 | Provider wire permits spans or confidence values that the server must reject | A complete candidate ledger still becomes unresolved and withholds the real submission channel | Wire v3 uses start plus length 1..500, decisive confidence >=0.9 with an explicit uncertainty outlet, and retains server boundary checks | backend | T11 in progress; no paid rerun before QA9 |
| R-49 | A model evaluation record conflicts with or is marked more broadly than a server-recovered field | Verified selection method is cleared despite source-grounded recovery | Give source-verified recovered evaluation fields precedence over model records for the same document and field; retain no-recovery controls | backend | T11 in progress; no paid rerun before QA9 |
| R-50 | Overlapping context windows independently own the same model-chosen relation | Equivalent source semantics produce different spans or signatures and globally block submission resolution | Partition each page into one-owner cores, retain bounded halo only as context, and verify deterministic ownership | backend | T12 in progress; no paid rerun before QA10 |
| R-51 | Provider inspect telemetry changes while request, response, and price contracts stay fixed | Whole-response hash blocks a safe deployment or encourages an unsafe bypass | Reinspect and record the exact response hash before deployment; separate telemetry from semantic contract in a reviewed follow-up | chief | current request schema and price unchanged; exact deployment receipt refreshed |
| R-52 | Core coverage is quantified over all procurement prose rather than delivery relations | Unrelated ambiguity marks otherwise exhaustive cores uncertain and withholds a real submission channel | Define completeness over the delivery-relation domain; retain explicit uncertainty only for plausible unresolved delivery relations, truncation, or capacity | backend | T12 production falsified old wording; T13 active, no paid rerun before QA11 |
| R-53 | Independent context-relative condition offsets form an invalid or ambiguous pair | One malformed condition makes an otherwise valid relation unresolved | Use one relation-relative condition object and server checked-add containment; reject legacy or malformed shapes | backend | T12 production observed one mismatch; T13 active, no paid rerun before QA11 |
| R-54 | Every edit reparses full tenders or reruns the entire test and production stack | Slow feedback, unnecessary provider cost, and time spent outside the changed subsystem | Four-level verification cadence with hash-bound local replay artifacts; focused tests during iteration and one full gate per release candidate | chief | active rule; T13 is first task under the new cadence |
